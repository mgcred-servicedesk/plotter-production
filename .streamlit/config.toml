# Configuracao do Streamlit para deploy no Streamlit Cloud
# Sem [theme] fixo — permite light/dark via toggle customizado

[server]
headless = true

[browser]
gatherUsageStats = false

[logger]
level = "info"
